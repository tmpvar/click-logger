to get started clone this repo and open a terminal in this directory and then run:

```
npm install
```

to get the dependencies, and to start run:

```
npm start
```

then navigate to http://localhost:8000/
